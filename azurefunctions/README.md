# CheckMK Azure Functions plugin

CheckMK special agent for monitoring Azure Functions invocations.

## Prerequisites

* CheckMK installation
* Python packages: `azure-identity`, `azure-mgmt-web`, `requests`, `aiohttp`, `croniter`
